<div class="row">
    <div class="col-md-6">
        {{#each items}}
            {{#unless this.completed}}
                <p>{{this.name}}</p>
            {{/unless}}
        {{/each}}
    </div>
    <div class="col-md-6">
        {{#each items}}
            {{#if this.completed}}
                <p>{{this.name}}</p>
            {{/if}}
        {{/each}}
    </div>
</div>